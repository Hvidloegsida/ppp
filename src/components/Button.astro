<a class="text-pink rounded-xl border-4 border-solid border-pink py-3 font-good md:text-[20px] xl:text-[50px] mb-7 mx-10 my-6 md:mx-32 xl:mx-44 xl:mb-[12rem] text-center slide" href="/gallery">Galleri</a>

<style>
/* Default state for .slide */
.slide {
  box-shadow: inset 0 0 0 0 #FBC7C7;
  color: #FBC7C7; /* Preserve the original text color */
  transition: box-shadow 0.25s, color 0.25s; /* Ensure the transition is applied */
}

/* Animation for .slide:hover and .slide:focus */
.slide:hover,
.slide:focus {
  box-shadow: inset 20em 0 0 0 #FBC7C7;
  color: #5B466C; /* Change the text color when hovered */
}

/* Basic button styles */
button {  
  color: #FBC7C7;
  transition: 0.25s;
  border: 2px solid;
  font: inherit;
  padding: 1em 2em;
}

/* Button hover and focus styles */
button:hover,
button:focus { 
  border-color: #FBC7C7;
  color: #fff;
}
</style>
